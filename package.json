{
  "name": "depguard-cli",
  "version": "1.14.0",
  "description": "MCP security server for AI coding agents. Workspace auto-exec audit (pre-open repo scan, defends against fake-interview / take-home-test malware), static code analysis, behavioral detection, pre-install guardian, AI hallucination guard, dead dependency detection, vulnerability audit, remediation planner, CycloneDX SBOM generation. 14 tools. Zero dependencies.",
  "author": "Jorge Morais",
  "funding": {
    "type": "github",
    "url": "https://github.com/sponsors/mopanc"
  },
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "depguard-cli": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "src/data",
    "README.md"
  ],
  "scripts": {
    "check-version": "node --import tsx scripts/check-version.ts",
    "build": "tsc && rm -rf dist/data && mkdir -p dist/data && cp src/data/*.json dist/data/ && chmod 755 dist/cli.js",
    "test": "node --import tsx --test tests/*.test.ts",
    "lint": "eslint src/ tests/",
    "audit:security": "npm audit --audit-level=high --omit=dev",
    "check": "npm run check-version && npm run build && npm run lint && npm test && npm run audit:security",
    "prepublishOnly": "npm run check"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^22.0.0",
    "eslint": "^9.39.4",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.57.0"
  },
  "keywords": [
    "npm",
    "audit",
    "security",
    "license",
    "dependency",
    "package",
    "vulnerability",
    "maintenance",
    "advisor",
    "sbom",
    "cyclonedx",
    "supply-chain"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mopanc/depguard"
  },
  "engines": {
    "node": ">=18"
  },
  "socket": {
    "network": {
      "reason": "Fetches package metadata from npm registry (registry.npmjs.org) and security advisories from GitHub Advisory Database (api.github.com). No other network access. All URLs are hardcoded constants — no dynamic URL construction.",
      "domains": [
        "registry.npmjs.org",
        "api.npmjs.org",
        "api.github.com"
      ]
    }
  }
}
